<div class="error-404-terminal">
  <div class="error-backdrop">
    <div class="glitch-overlay"></div>
  </div>

  <div class="error-content">
    <p class="error-code">ERROR_404 // SIGNAL_LOST</p>
    <h1 class="error-title glitch-text" data-text="TRANSMISSION FAILED">TRANSMISSION FAILED</h1>
    
    <div class="error-terminal-readout">
      <p class="log-line">> ATTEMPTING_RECOVERY...</p>
      <p class="log-line">> TARGET_SECTOR_NOT_FOUND.</p>
      <p class="log-line">> SCANNING_VOID_FOR_RELICS...</p>
      <p class="log-line terminal-cursor">_</p>
    </div>

    <div class="error-actions">
      <a href="/collections/all" class="btn-solid btn-override">
        INITIATE_MANUAL_OVERRIDE
      </a>
      <p class="error-hint">Return to main database for extraction.</p>
    </div>
  </div>
</div>

<style>
  .error-404-terminal {
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #000;
    color: var(--neon-magenta);
    text-align: center;
    font-family: var(--font-mono);
  }
  .error-code { font-size: 0.8rem; color: var(--chrome-silver); margin-bottom: 1rem; }
  .error-title { font-family: var(--font-title); font-size: 4rem; margin-bottom: 2rem; }
  .error-terminal-readout {
    background: rgba(225, 60, 250, 0.05);
    border: 1px solid var(--neon-magenta);
    padding: 2rem;
    margin-bottom: 3rem;
    max-width: 450px;
    text-align: left;
  }
  .btn-override {
    border: 1px solid var(--neon-magenta);
    color: var(--neon-magenta);
    padding: 1rem 2rem;
    text-decoration: none;
    transition: all 0.3s ease;
  }
  .btn-override:hover {
    background: var(--neon-magenta);
    color: #000;
    box-shadow: 0 0 20px var(--neon-magenta);
  }
</style>