{% comment %}
STELLAR REVERB — UNIVERSAL LORE HERO
Pulse-glow background, glitch title, CTA button
Dynamic metaobject integration for featured capsule
{% endcomment %}

<section id="lore-hero" class="lore-hero" data-section-type="lore-hero" role="banner">
  <style>
    #lore-hero{position:relative;min-height:92vh;display:flex;align-items:center;justify-content:center;border-bottom:1px solid var(--cyan-blue,#28A8D6);overflow:hidden;background:var(--void-black,#0A0A0A);color:#fff}
    .hero-bg{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;opacity:0.58;filter:brightness(0.9) contrast(1.05)}
    .hero-overlay{position:absolute;inset:0;background:linear-gradient(180deg,rgba(10,10,10,0) 40%,rgba(10,10,10,.55) 72%,rgba(10,10,10,.8) 100%)}
    .hero-pulse{position:absolute;inset:0;background:radial-gradient(circle at 20% 50%, rgba(225,60,250,0.12) 0%, transparent 50%), radial-gradient(circle at 80% 50%, rgba(40,168,214,0.12) 0%, transparent 50%);animation:pulse-bg 8s ease-in-out infinite}
    @keyframes pulse-bg{0%,100%{opacity:.30}50%{opacity:.60}}
    .hero-content{position:relative;z-index:2;text-align:center;padding:2rem;max-width:1200px}
    .hero-title{font-family:'Orbitron',monospace;font-weight:900;letter-spacing:0.1em;font-size:clamp(2rem,8vw,4.75rem);text-transform:uppercase;color:var(--neon-magenta,#E13CFA);text-shadow:0 0 20px var(--neon-magenta,#E13CFA);animation:glitch-anim 3s infinite}
    @keyframes glitch-anim{0%,90%,100%{transform:translate(0)}92%{transform:translate(-2px,2px)}94%{transform:translate(2px,-2px)}96%{transform:translate(-2px,-2px)}}
    .hero-subtitle{font-family:'Montserrat',sans-serif;font-weight:400;letter-spacing:0.06em;color:var(--cyan-blue,#28A8D6);margin:1.25rem 0 2rem;font-size:clamp(1rem,3vw,1.25rem)}
    .hero-description{max-width:600px;margin:0 auto 2rem;font-family:'Montserrat',sans-serif;color:rgba(255,255,255,.85);line-height:1.6;font-size:1rem}
    .cta-primary{font-family:'Orbitron',monospace;background:var(--radioactive-green,#00F0AC);color:#0a0a0a;border:none;padding:1rem 1.75rem;border-radius:4px;cursor:pointer;text-transform:uppercase;font-weight:800;letter-spacing:0.08em;transition:transform 0.25s ease, box-shadow 0.25s ease}
    .cta-primary:hover{transform:translateY(-2px);box-shadow:0 0 28px var(--radioactive-green,#00F0AC);filter:brightness(1.1)}
    @media (prefers-reduced-motion:reduce){.hero-pulse, .hero-title{animation:none}}
  </style>

  <img class="hero-bg" src="https://via.placeholder.com/2000x1000/0A0A0A/E13CFA?text=Cosmic+Void" alt="Neon cosmic background" loading="eager" fetchpriority="high">
  <div class="hero-overlay" aria-hidden="true"></div>
  <div class="hero-pulse" aria-hidden="true"></div>

  <div class="hero-content">
    <h1 class="hero-title">UNIVERSAL LORE: SIGNALS FROM THE VOID</h1>
    <p class="hero-subtitle">Distort reality, wear the echo</p>
    <p class="hero-description">Post-singularity transmission archive. {{ metaobjects.capsule.values | size }} capsules of pure cosmic lore.</p>
    <button class="cta-primary" id="enterVoid" onclick="document.getElementById('lore-timeline').scrollIntoView({behavior:'smooth'})">Enter the transmission</button>
  </div>

  <script type="application/ld+json">
  {"@context":"https://schema.org","@type":"CreativeWork","name":"Universal Lore: Signals from the Void","creator":{"@type":"Organization","name":"{{ shop.name | escape }}"},"description":"Post‑singularity lore: analog mysticism × cosmic surrealism × glitch futurism.","url":"{{ request.origin }}{{ request.path }}","inLanguage":"en"}
  </script>
</section>

{% schema %}
{"name":"Lore Hero","settings":[]}
{% endschema %}
