/* ===== SUPREMACY HERO OVERLAY + TEXT POP ===== */

/* 1) Add a dark + neon overlay on top of the hero image */
.hero-banner {
  position: relative;
  overflow: hidden;
}

.hero-banner::before {
  content: "";
  position: absolute;
  inset: 0;
  /* Darken the image + add subtle neon gradients */
  background:
    radial-gradient(circle at 0% 0%, rgba(0, 247, 255, 0.20), transparent 55%),
    radial-gradient(circle at 100% 100%, rgba(255, 0, 166, 0.20), transparent 55%),
    linear-gradient(to bottom, rgba(0, 0, 0, 0.65), rgba(0, 0, 0, 0.90));
  pointer-events: none;
  z-index: 0;
}

/* 2) Make sure all hero content sits ABOVE the overlay */
.hero-banner * {
  position: relative;
  z-index: 1;
}

/* 3) Make the main heading feel like it’s floating off the screen */
.hero-banner .banner__heading,
.hero-banner h1 {
  text-shadow:
    0 0 18px rgba(0, 0, 0, 0.9),
    0 0 32px rgba(0, 0, 0, 0.95);
}

/* 4) Increase readability of the subtitle line */
.hero-banner .banner__text,
.hero-banner .subtitle {
  max-width: 640px;
  margin-left: auto;
  margin-right: auto;
  text-shadow: 0 0 12px rgba(0, 0, 0, 0.85);
}

/* 5) Give the primary CTA that neon “click me” glow */
.hero-banner .button,
.hero-banner .button--primary {
  box-shadow: 0 0 24px rgba(0, 247, 255, 0.5);
  transform: translateZ(0);
}

/* Slight hover lift for extra depth */
.hero-banner .button:hover,
.hero-banner .button--primary:hover {
  transform: translateY(-1px);
  box-shadow: 0 0 32px rgba(0, 247, 255, 0.7);
}