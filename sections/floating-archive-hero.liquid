{% comment %}
  =====================================================
  BEAST MODE HERO: CINEMATIC ASSET SYNC
  Using specific Shopify File URL for reliability
  =====================================================
{% endcomment %}

<section class="floating-archive-hero" data-section-id="{{ section.id }}">
  <div class="hero-relic-bg">
    {% comment %} Corrected to use your specific Shopify file name {% endcomment %}
    <img src="{{ 'stellar-universal-lore-hero-image_219d5e8d-4dc4-44b4-967a-8144fb637b55.jpg' | file_img_url: '2000x' }}" 
         alt="Archive Hero Relic" 
         class="hero-bg-image"
         loading="eager">
    <div class="hero-vignette"></div>
    <div class="hero-scanlines"></div>
  </div>

  <div class="hero-container">
    <div class="hero-content">
       <h1 class="hero-title glitch-text" data-text="{{ section.settings.hero_title | default: 'THE FLOATING ARCHIVE' }}">
         {{ section.settings.hero_title | default: 'THE FLOATING ARCHIVE' }}
       </h1>
       <p class="hero-subtitle">{{ section.settings.hero_subtitle | default: 'UNINDEXED RELICS FROM THE VOID' }}</p>
    </div>
  </div>
</section>