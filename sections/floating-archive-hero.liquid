{% comment %}
  =====================================================
  BEAST MODE HERO: CINEMATIC ASSET INTEGRATION
  =====================================================
{% endcomment %}

<section class="floating-archive-hero" data-section-id="{{ section.id }}">
  {% comment %} The "Relic" Background Image {% endcomment %}
  <div class="hero-relic-bg">
    <img src="{{ 'F575AEE6-7A78-4A89-AA9B-BAFAC0243FFF.jpg' | asset_url }}" 
         alt="Archive Hero Relic" 
         class="hero-bg-image"
         loading="eager">
    <div class="hero-vignette"></div>
    <div class="hero-scanlines"></div>
  </div>

  <div class="hero-container">
    {% comment %} Your existing Hologram Frame and Content logic {% endcomment %}
    <div class="hero-content">
       <h1 class="hero-title glitch-text" data-text="{{ section.settings.hero_title }}">
         {{ section.settings.hero_title | default: 'THE FLOATING ARCHIVE' }}
       </h1>
       <p class="hero-subtitle">{{ section.settings.hero_subtitle }}</p>
    </div>
  </div>
</section>