<!doctype html>
<html lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>{{ page_title }}</title>

  {% if page_description %}<meta name="description" content="{{ page_description | escape }}">{% endif %}
  <link rel="canonical" href="{{ canonical_url }}">
  <link rel="preconnect" href="https://cdn.shopify.com" crossorigin>

  {%- comment -%} 1. BRAND FONTS & CSS ENGINE {%- endcomment -%}
  {{ 'brand-fonts.css' | asset_url | stylesheet_tag }}
  {{ 'theme-variables.css' | asset_url | stylesheet_tag }}
  {{ 'base.css' | asset_url | stylesheet_tag }}

  {%- comment -%} 2. CORE STYLE OVERRIDES {%- endcomment -%}
  <style>
    :root {
      --font-title: 'Orbitron', sans-serif;
      --font-body: 'PP Mori', sans-serif;
      --font-display: 'PP Neue Machina', serif;
    }
    body { font-family: var(--font-body); background: #0D0D0D; color: #FFF; }
    h1, h2, h3, .orbitron { font-family: var(--font-title); text-transform: uppercase; letter-spacing: 0.1em; }
    .display-text { font-family: var(--font-display); }
  </style>

  {{ content_for_header }}
</head>

<body class="template-{{ template.name | handle }} stellar-os-active">

  {%- comment -%} 3. BOOT SEQUENCE HANDSHAKE {%- endcomment -%}
  {%- if template == 'index' -%}
    {% render 'stellar-boot-sequence' %}
  {%- endif -%}

  <main id="MainContent" class="main-content">
    {{ content_for_layout }}
  </main>

  {%- comment -%} 4. GLOBAL MASTER ENGINE - TURBOCHARGED {%- endcomment -%}
  {%- comment -%} Replace individual archive scripts with this consolidated core {%- endcomment -%}
  <script src="{{ 'stellar-core.js' | asset_url }}" defer></script>
  
  {%- comment -%} 5. PAGE-SPECIFIC LORE ENGINE {%- endcomment -%}
  {%- if template contains 'lore' -%}
    <script src="{{ 'stellar-lore-legendary.js' | asset_url }}" defer></script>
  {%- endif -%}

</body>
</html>