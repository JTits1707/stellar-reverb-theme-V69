<!doctype html>
<html class="no-js" lang="{{ request.locale.iso_code }}">
  <head>
    {%- comment -%} 1. PERFORMANCE: Critical Lore CSS (Inlined for <1s FCP) {%- endcomment -%}
    <style>
  {% render 'critical-lore-css' %}
</style>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>
      {%- if page_title == blank -%}{{ shop.name }}{%- else -%}{{ page_title }}{% unless page_title contains shop.name %} – {{ shop.name }}{% endunless %}{%- endif -%}
    </title>
    {%- if page_description -%}<meta name="description" content="{{ page_description | escape }}">{%- endif -%}
    <meta name="theme-color" content="#0D0D0D">
    <link rel="icon" type="image/png" href="{{ 'favicon.png' | asset_url }}">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Inter:wght@300;400;700&display=swap" rel="stylesheet">

    {%- comment -%} 2. CORE CSS ENGINE: Global Brand Identity {%- endcomment -%}
    {{ 'theme-variables.css' | asset_url | stylesheet_tag }}
    {{ 'brand-fonts.css' | asset_url | stylesheet_tag }}
    {{ 'base.css' | asset_url | stylesheet_tag }}

    {%- comment -%} 3. PAGE-SPECIFIC LORE: The Multiversal Stack {%- endcomment -%}
    {% if template contains 'lore' %}
      {{ 'universal-lore.css' | asset_url | stylesheet_tag }}
      {{ 'lore-mainframe.css' | asset_url | stylesheet_tag }}
      {{ 'lore-myth.css' | asset_url | stylesheet_tag }}
      {{ 'lore-timeline.css' | asset_url | stylesheet_tag }}
      {{ 'lore-vault.css' | asset_url | stylesheet_tag }}
      {{ 'stellar-timeline-cosmic.css' | asset_url | stylesheet_tag }}
    {% endif %}

    {% if template contains 'collection' %}
      {{ 'stellar-shop-all.css' | asset_url | stylesheet_tag }}
    {% endif %}

    {%- comment -%} 4. FINAL BOSS OVERRIDE: Terminating Ghost Space & Forcing Fonts {%- endcomment -%}
    <style>
      :root {
        --font-heading: 'Orbitron', sans-serif !important;
        --font-human: 'PPMORI', 'Inter', sans-serif !important;
        --font-ai: 'PP Neue Machina', monospace !important;
      }
      .main-content { padding-top: 0 !important; margin-top: 0 !important; }
      #MainContent section:first-of-type { margin-top: 0 !important; }
      
      /* Ensure Myth buttons sit horizontally */
      .toggle-switch-wrapper { display: flex !important; gap: 2rem !important; justify-content: center !important; }
    </style>

    {{ content_for_header }}
  </head>

  <body class="template-{{ template.name | handle }} stellar-os-active">
    {%- comment -%} 🛸 UNIVERSAL BOOT SEQUENCE {%- endcomment -%}
    {%- if template == 'index' -%}
      {% render 'stellar-boot-sequence' %}
    {%- endif -%}

    {%- comment -%} 📡 GLOBAL HANDSHAKE TERMINAL {%- endcomment -%}
    {% render 'stellar-handshake' %}

    {% section 'header-enhanced' %}

    <main id="MainContent" role="main" class="main-content">
      {{ content_for_layout }}
    </main>

    {% section 'stellar-footer' %}

    {%- comment -%} ═══════════════════════════════════════════════════════════
                   JAVASCRIPT - THE MASTER HANDSHAKE
    ═══════════════════════════════════════════════════════════ {%- endcomment -%}
    
    <script src="{{ 'stellar-core.js' | asset_url }}" defer></script>
    
    {% if template contains 'lore' %}
      <script src="{{ 'universal-lore.js' | asset_url }}" defer></script>
      <script src="{{ 'stellar-timeline-cosmic.js' | asset_url }}" defer></script>
    {% endif %}
    
    {% if template contains 'collection' %}
      <script src="{{ 'stellar-shop-all.js' | asset_url }}" defer></script>
    {% endif %}
    
    <script src="{{ 'signal-seeker-terminal.js' | asset_url }}" defer></script>
  </body>
</html>
{% comment %} Load Archive CSS when in sector {% endcomment %}
{% if template contains 'floating-archive' %}
  {{ 'floating-archive-enhanced.css' | asset_url | stylesheet_tag }}
{% endif %}

{% comment %} Load Archive JS at bottom {% endcomment %}
{% if template contains 'floating-archive' %}
  <script src="{{ 'floating-archive.js' | asset_url }}" defer></script>
{% endif %}